import { SearchOppoPipe } from './search-oppo.pipe';

describe('SearchOppoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchOppoPipe();
    expect(pipe).toBeTruthy();
  });
});
