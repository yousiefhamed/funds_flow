import { SearchNameUserPipe } from './search-name-user.pipe';

describe('SearchNameUserPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchNameUserPipe();
    expect(pipe).toBeTruthy();
  });
});
